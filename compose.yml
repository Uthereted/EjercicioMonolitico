services:

  order-service:

    build: fastapi_app

    ports:
      - "13001:8000"

    volumes:
      - "./db_volume:/volume"

    environment:
      PYTHONUNBUFFERED: 1
      SQLALCHEMY_DATABASE_URL: sqlite+aiosqlite:////volume/orders.db

    restart: on-failure


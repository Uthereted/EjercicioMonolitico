meta {
  name: Remove an order
  type: http
  seq: 3
}

delete {
  url: {{baseURL}}/order/{{orderId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.id: eq {{orderId}}
}

settings {
  encodeUrl: true
}

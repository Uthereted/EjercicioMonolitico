meta {
  name: View all orders
  type: http
  seq: 4
}

get {
  url: {{baseURL}}/order
  body: none
  auth: inherit
}

vars:post-response {
  orderId: res('[0].id')
}

assert {
  res.status: eq 200
  res.body: isArray
}

settings {
  encodeUrl: true
}
